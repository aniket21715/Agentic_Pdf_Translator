[pytest]
testpaths = tests
; Tells pytest to only look in the tests/ folder — prevents it from scanning your entire project
addopts = -p no:cacheprovider
; Disables pytest's cache plugin — that's what prevents those pytest-cache-files-* junk folders from being created
